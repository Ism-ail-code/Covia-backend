# common/

Helpers shared across modules and infrastructure layers — response
pagination DTOs, shared value objects, small utilities that depend on
NestJS primitives.

Use `src/utils/` for pure functions without NestJS dependencies.
