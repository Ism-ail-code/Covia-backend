# services/

Long-lived, application-wide services that do not belong to one feature
module (e.g. notification dispatch, file storage, maps integration).

Phase 1 defines none; the directory is reserved for future phases.
